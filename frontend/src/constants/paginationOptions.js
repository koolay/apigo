/**
 * 数据分页大小
 */
export const PAGE_SIZE = 10

/**
 * 最多显示的分页按钮数
 */
export const MAX_BUTTONS = 5

/**
 * 默认显示数据总数
 */
export const SHOW_TOTAL_TEXT = true

/**
 * 这里导出默认的分页设置，方便后面使用
 */
export const DEFAULT_PAGINATION_OPTIONS = {
	pageSize: PAGE_SIZE,
	maxButtons: MAX_BUTTONS,
	showTotalText: true
}